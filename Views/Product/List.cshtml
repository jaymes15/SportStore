@model IEnumerable<Product>
@{
    ViewData["Title"] = "List";
}

<h1>List</h1>

@foreach (var product in Model)
{
    <div>
        <h3>@product.Name</h3>
        @product.Description
        <h4>@product.Price</h4>
    </div>
}